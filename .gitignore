.journals
.tmp
.worktrees
.env
.scratch
.agents/*
# Curated CES rule docs are authored content referenced by the tracked AGENTS.md index — keep
# them versioned even though installed agent skills under .agents/ stay ignored.
!.agents/rules/
skills-lock.json

# python / build
.venv/
dist/
build/
*.egg-info/
__pycache__/
.pytest_cache/
.ruff_cache/
!.env.schema
.tmp/
.scratch/
.worktrees/
.journals/
